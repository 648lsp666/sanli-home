# Blog System Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a `/blog` section with list + detail pages and 5 written articles, integrated into the existing sanli-home brutalist design.

**Architecture:** Markdown files in `content/posts/` parsed server-side with `gray-matter` + `marked`. Blog list page is a Next.js Server Component that passes post metadata to a `'use client'` PostCard component for bilingual rendering. Post detail page is an async Server Component (Next.js 16 requires `await params`).

**Tech Stack:** Next.js 16 App Router, gray-matter ^4, marked ^12, existing LangProvider + next-themes, Tailwind v4, Fusion Pixel font.

---

## File Map

| File | Action | Purpose |
|------|--------|---------|
| `content/posts/ai-exit-path.md` | Create | Article 1 |
| `content/posts/30-days-countdown.md` | Create | Article 2 |
| `content/posts/claude-code-thesis.md` | Create | Article 3 |
| `content/posts/prowl-retrospective.md` | Create | Article 4 |
| `content/posts/bytedance-week-one.md` | Create | Article 5 |
| `lib/posts.ts` | Create | `getAllPosts()`, `getPost(slug)` |
| `app/globals.css` | Modify | Add `.prose` typography block |
| `components/PostCard.tsx` | Create | Client component — bilingual card |
| `components/BlogPostHeader.tsx` | Create | Client component — bilingual post header/back btn |
| `app/blog/page.tsx` | Create | Blog list Server Component |
| `app/blog/[slug]/page.tsx` | Create | Post detail async Server Component |
| `lib/content.ts` | Modify | Update ARTICLES URLs from `#` to `/blog/[slug]` |

---

## Task 1: Install Dependencies

**Files:** `package.json`, `package-lock.json`

- [ ] **Step 1: Install packages**

```bash
cd /Users/sanli/Desktop/workplace/ai-programme/workspace/projects/sanli-home
npm install gray-matter marked
npm install --save-dev @types/marked
```

Expected: packages added to `node_modules`, no errors.

- [ ] **Step 2: Verify install**

```bash
node -e "require('gray-matter'); require('marked'); console.log('OK')"
```

Expected output: `OK`

- [ ] **Step 3: Commit**

```bash
git add package.json package-lock.json
git commit -m "chore: add gray-matter and marked"
```

---

## Task 2: Create `lib/posts.ts`

**Files:**
- Create: `lib/posts.ts`

- [ ] **Step 1: Create the file**

```typescript
// lib/posts.ts
import fs from 'fs'
import path from 'path'
import matter from 'gray-matter'
import { marked } from 'marked'

const POSTS_DIR = path.join(process.cwd(), 'content/posts')

export interface Post {
  slug: string
  title: string
  titleEn: string
  date: string
  category: string
  categoryEn: string
  categoryColor: string
  readTime: string
  excerpt: string
  excerptEn: string
  tags: string[]
  content?: string
}

export function getAllPosts(): Post[] {
  const files = fs.readdirSync(POSTS_DIR).filter(f => f.endsWith('.md'))
  return files
    .map(filename => {
      const raw = fs.readFileSync(path.join(POSTS_DIR, filename), 'utf-8')
      const { data } = matter(raw)
      return data as Post
    })
    .sort((a, b) => (a.date < b.date ? 1 : -1))
}

export function getPost(slug: string): Post {
  const filepath = path.join(POSTS_DIR, `${slug}.md`)
  const raw = fs.readFileSync(filepath, 'utf-8')
  const { data, content } = matter(raw)
  return {
    ...(data as Post),
    content: marked(content) as string,
  }
}
```

- [ ] **Step 2: Commit**

```bash
git add lib/posts.ts
git commit -m "feat: add lib/posts.ts with getAllPosts and getPost"
```

---

## Task 3: Add `.prose` Typography to `globals.css`

**Files:**
- Modify: `app/globals.css`

- [ ] **Step 1: Append to the end of `app/globals.css`**

```css
/* ── Blog prose typography ── */
.prose { color: var(--color-ink); max-width: 680px; }
.prose h2 { font-size: 22px; font-weight: 900; margin: 2.2rem 0 0.75rem; padding-bottom: 4px; border-bottom: 3px solid var(--color-accent-green); }
.prose h3 { font-size: 17px; font-weight: 900; margin: 1.75rem 0 0.5rem; }
.prose p  { font-size: 15px; line-height: 1.9; margin: 0 0 1.2rem; opacity: 0.8; }
.prose strong { font-weight: 900; opacity: 1 !important; }
.prose em { font-style: italic; }
.prose ul, .prose ol { margin: 0 0 1.2rem 1.5rem; }
.prose ul { list-style-type: disc; }
.prose ol { list-style-type: decimal; }
.prose li { font-size: 15px; line-height: 1.75; margin-bottom: 0.4rem; opacity: 0.8; }
.prose blockquote { border-left: 4px solid var(--color-accent-cyan); padding-left: 1rem; margin: 1.5rem 0; font-style: italic; }
.prose blockquote p { opacity: 0.65; }
.prose code { font-family: var(--font-mono); font-size: 13px; background: var(--color-paper); padding: 2px 6px; border: 1.5px solid var(--color-ink); border-radius: 0; }
.prose pre { background: var(--color-paper); border: 2px solid var(--color-ink); box-shadow: 3px 3px 0 var(--color-ink); padding: 0; margin: 0 0 1.5rem; overflow-x: auto; }
.prose pre code { display: block; padding: 1rem; border: none; background: none; font-size: 13px; }
.prose hr { border: none; border-top: 2px solid var(--color-ink); opacity: 0.15; margin: 2.5rem 0; }
.prose a { border-bottom: 2px solid var(--color-accent-green); opacity: 0.85; }
.prose a:hover { opacity: 1; }
```

- [ ] **Step 2: Commit**

```bash
git add app/globals.css
git commit -m "feat: add prose typography styles for blog"
```

---

## Task 4: Write Article Content

**Files:**
- Create: `content/posts/ai-exit-path.md`
- Create: `content/posts/30-days-countdown.md`
- Create: `content/posts/claude-code-thesis.md`
- Create: `content/posts/prowl-retrospective.md`
- Create: `content/posts/bytedance-week-one.md`

- [ ] **Step 1: Create directory**

```bash
mkdir -p content/posts
```

- [ ] **Step 2: Create `content/posts/ai-exit-path.md`**

```markdown
---
slug: ai-exit-path
title: AI 时代独立开发者的副业脱身路径推演
titleEn: Simulating the AI-Era Indie Exit Path
date: 2026-05-25
category: 精选
categoryEn: Featured
categoryColor: "var(--color-accent-green)"
readTime: 12 min
excerpt: 模拟分析 2026 年副业转正的可行路径——黄金笼子、AI 自媒体、独立产品三条线的真实胜算。confidence 7/10。
excerptEn: Simulating viable paths to going independent in 2026 — golden cage, AI media, indie product. confidence 7/10.
tags: [AI, 副业, 推演]
---

7月入职字节跳动，月薪约 3.2 万，五险一金，大厂光环。按理说，我应该很满足。

但我不满足。

不是因为钱不够多，而是因为我非常清楚这条路通向哪里——五年后，我会成为一个离不开大厂的人。

所以我花了几天时间，认真模拟了三条路径，试图搞清楚「脱身」这件事的真实胜算。

---

## 路径一：黄金笼子（默认失败路径）

这条路不需要主动选择，它是默认发生的。

入职第一年，薪资 3.2 万，花销膨胀到 2 万。第三年涨薪到 5 万，花销跟着涨到 3.5 万。房租、生活品质、社交消费，会悄无声息地填满你所有的余粮。

五年后，你的生活成本结构已经和大厂绑定。这时候「出去」意味着接受 30-50% 的降薪，意味着砍掉很多你已经习以为常的消费。

**胜算：0%。** 不是能力问题，是人性问题。除非主动破局，否则这条路会一直走下去。

---

## 路径二：AI 自媒体（3 年可行）

这条路我觉得是三条路里最稳的。

逻辑很简单：我在做的事情（用 AI 工具开发副业、探索 AI 编程工作流）本身就是内容。把做的事情记录下来，就是内容创作。

具体规划：

- **第 1 年（2026-2027）**：每月输出 4-6 篇高质量技术+副业内容。小红书 + 公众号双线运营。目标：粉丝 5000，建立基础信任。
- **第 2 年（2027-2028）**：选择一个垂直方向深耕（AI 编程效率 or 独立开发工作流）。目标：粉丝 2 万，开始有广告询单。
- **第 3 年（2028-2029）**：知识付费 + 广告 + 私域，月收入达到 1-1.5 万。**这时候，副业可以覆盖 30-40% 的生活成本，「脱身」开始具有可能性。**

**胜算：7/10。** 最大的风险是坚持不下去。内容创作的前期回报极低，需要在看不到结果的时候持续输出。

---

## 路径三：独立产品（高风险，高上限）

见微 Prowl 是这条路的第一步。

独立产品的逻辑是：用技术杠杆做出能自动赚钱的东西，实现真正的时间解放。

但独立产品有个残酷的事实：**大部分产品活不过三个月。** 不是技术不行，而是找不到真实愿意付费的用户。

我对这条路的判断：
- 短期（1-2 年）：不要指望产品独立养活自己，把它当作学习和探索的工具
- 中期（3 年）：如果能做出一个月收入 3000 元以上的产品，就值得继续加注
- 长期：独立产品的上限最高，但方差也最大

**胜算：4/10。** 难在找到真实需求，并且坚持打磨。

---

## 结论：双线并行，3 年窗口期

综合来看，我的计划是：

**AI 自媒体（主线）+ 独立产品（副线）**，用自媒体积累流量和信任，用独立产品探索变现上限。

3 年内的里程碑：
1. 2026 年底：博客稳定更新，Prowl 第二版上线
2. 2027 年底：某平台粉丝破万，独立产品月收入 > 1000 元
3. 2028 年底：副业月收入 > 1.5 万，认真评估是否脱身

**整体 confidence：7/10。**

最大的不确定性不在技术，不在方向，在于「入职之后还有没有时间和精力」。大厂的消耗能力，我还没有亲身体验过。

这篇文章算是一个承诺。两年后回来看，看看推演准不准。
```

- [ ] **Step 3: Create `content/posts/30-days-countdown.md`**

```markdown
---
slug: 30-days-countdown
title: 入职倒计时 30 天，我在想什么
titleEn: 30 Days to Onboarding — What I'm Thinking
date: 2026-05-28
category: 碎碎念
categoryEn: Notes
categoryColor: "var(--color-accent-pink)"
readTime: 5 min
excerpt: 距离字节跳动入职还有约 30 天。复杂的心情、想完成的事、以及对未来的一点点不安。
excerptEn: About 30 days left before joining ByteDance. Mixed feelings, things I want to finish, and a touch of unease.
tags: [字节, 入职, 碎碎念]
---

今天是 2026 年 5 月 28 日。

7 月 1 日入职字节跳动，还有大概 33 天。

我坐在家里，桌上摊着论文和几个没写完的代码文件，想着要不要写点什么记录一下这段时间的心情。结果发现，「写点什么」这件事本身就挺能说明问题——我显然不在状态。

---

## 现在的心情

准确说，是几种情绪混在一起，我自己都有点分不清楚。

**有期待。** 大厂的工程规模、代码质量、同事水平，这些我确实很好奇。实习的时候有一点接触，感觉很多东西我都不会，这既让我焦虑，也让我觉得「至少能学到东西」。

**有忐忑。** 毕业了就是正式的了，不再是「实习生，出了问题可以理解」。从第一天起就得按正式员工的标准来。我不知道自己准备好了没有。

**有一点点不甘。** 这个我很少说出来，但它就是在那里。我知道自己想要的不只是打工，我想做出点自己的东西。但现实是，未来相当长一段时间，我的主要身份还是字节的员工。

这没什么问题，只是需要接受。

---

## 入职前想完成的事

时间不多了，我给自己列了几件优先级最高的事：

**第一：把博客搭起来。** 就是你现在看到的这个。我一直觉得有一个地方能记录自己的想法很重要，这比在朋友圈发碎碎念更有意义一点。

**第二：Prowl 第二版。** 第一版太简陋了，交互体验不好，我想在入职前至少把核心功能打磨一下。

**第三：把 Claude Code 的工作流整理成文档。** 过去几个月用下来积累了很多经验，应该写出来，不然又会忘。

这三件事能做完两件我就满足了。

---

## 对入职后的预期

我问过几个已经在字节的朋友，综合他们的反馈，我预期：

- **节奏很快**，需求排期很紧，deadline 是认真的
- **Code Review 很严**，每一行代码都会被仔细看
- **文档文化好**，重要的事情都有记录
- **不会有人手把手带你**，得自己找资源解决问题

这些我觉得我还 OK。我比较怕的是另一件事：**彻底失去做自己事情的时间和意愿。**

入职之后，如果每天下班就只想躺平，那副业的事就很难推进了。这不是懒不懒的问题，是人的能量是有限的。

我现在还不知道答案，只能走一步看一步。

---

33 天，挺短的。

但不管怎样，先好好过完这段时间吧。毕业、入职，这些事只有一次。
```

- [ ] **Step 4: Create `content/posts/claude-code-thesis.md`**

```markdown
---
slug: claude-code-thesis
title: 用 Claude Code 做毕业论文格式化
titleEn: Using Claude Code for Thesis Formatting
date: 2026-05-20
category: 技术
categoryEn: Tech
categoryColor: "#888"
readTime: 8 min
excerpt: 四步流程，用 AI 处理掉了 6 小时的论文格式手动工作。包括字体、页码、参考文献、上标引用。
excerptEn: A four-step pipeline that saved 6 hours of manual thesis formatting — fonts, page numbers, citations, and superscripts.
tags: [Claude Code, 论文, AI工具]
---

毕业论文的格式要求是我见过的最反人类的文档之一。

华中科技大学的规范大概是这样的：正文小四宋体，标题三号黑体，页码前言用罗马数字、正文用阿拉伯数字，参考文献要按引用顺序编号，引用处要用上标……

每一条单独看都不难，但组合在一起，在一个几万字的 Word 文档里操作，就是噩梦。

我最开始打算手动改，改了两页就放弃了，转而让 Claude Code 来处理这件事。效果出乎意料地好。

---

## 总体思路

Claude Code 处理 Word 文档的方式是通过 Python 的 `python-docx` 库操作 `.docx` 文件的 XML 结构。我的整个流程分四步，每步解决一个格式问题。

---

## 第一步：正文字体统一

**任务：** 把所有正文段落的字体改为小四（12pt）宋体。

这步看起来简单，难点在于 Word 文档里的字体设置极其混乱——每个段落可能有不同的 Run，每个 Run 的字体设置可能在段落级别、Run 级别、或者主题字体里。直接遍历改会漏掉很多。

Claude Code 写了一个递归遍历所有段落 Run 的脚本，统一设置 `font.name`（中文用宋体、西文用 Times New Roman）和 `font.size`（Pt(12)）。

运行之后，整篇论文的正文字号终于一致了。

---

## 第二步：页码分节设置

**任务：** 前言部分（目录、摘要）用罗马数字，第一章之后用阿拉伯数字，且阿拉伯数字从 1 开始重新计数。

这是最麻烦的一步。Word 的页码是通过「节」（Section）来控制的，需要在正确的位置插入分节符，并分别设置每节的页码格式。

`python-docx` 对页码的支持很有限，Claude Code 的方案是直接操作底层 XML，在需要的地方注入 `<w:pgNumType>` 节点，指定格式和起始页码。

这步调试了两轮——第一次罗马数字部分的页码位置对了，但阿拉伯数字没有从 1 重新开始；第二次修正了起始值参数之后才完全正确。

---

## 第三步：参考文献重排序 + 引用编号重新整理

**任务：** 参考文献必须按正文中出现的先后顺序排列，且文末参考文献列表的编号要和正文引用的标注一致。

这步的逻辑分两部分：

1. **扫描正文**，找出所有引用标注（格式是 `[数字]`），记录每个引用第一次出现的顺序
2. **重排参考文献列表**，按第一次出现的顺序重新排列，并更新正文里的所有引用编号

这个脚本写起来比较复杂，因为有的引用是「[1,2]」「[3-5]」这种合并格式，需要单独处理。

最终效果是正确的，参考文献从原来按添加顺序变成了按引用顺序，所有编号保持一致。

---

## 第四步：引用改上标 + 公式符号规范化

**任务：** 正文引用从 `[1]` 格式改为上标格式，同时统一规范公式里的变量符号格式。

引用改上标相对简单，找到所有 `[数字]` 的 Run，把它们的 `font.superscript` 设为 `True`，删掉方括号即可。

公式符号规范化是额外的需求——论文里有些公式用的是普通文字，有些用的是 Word 公式编辑器。这步主要是把用普通文字写的变量（如斜体的「x」「y」）替换成统一格式，保证视觉一致性。

---

## 最终结果

四步跑完，生成了 `格式规范后的论文_V4_引用上标.docx`。

人工校对花了大概 45 分钟，主要是检查几个边缘情况（引用在图注里的情况、跨段落的公式）。

如果手动做这些事，我估计需要 5-8 小时，还容易出错。用 Claude Code 的方式，加上调试和校对，总共花了不到 2 小时。

---

## 一点反思

这次经历让我确认了一件事：**Claude Code 处理「有明确规则的结构化任务」非常擅长。** 只要你能清楚地描述规则，它基本上都能找到实现方式。

难点不在 AI，在于你自己要弄清楚规则是什么。论文格式这件事，我自己也是在说清楚规则的过程中，才真正理解了 Word 的页码机制。

如果你也有类似的格式化需求，可以直接让 Claude Code 帮你写 python-docx 脚本，比你自己查文档快得多。
```

- [ ] **Step 5: Create `content/posts/prowl-retrospective.md`**

```markdown
---
slug: prowl-retrospective
title: 见微 Prowl 上线复盘
titleEn: Prowl Launch Retrospective
date: 2026-05-15
category: 产品
categoryEn: Product
categoryColor: "#888"
readTime: 7 min
excerpt: 从想法到上线两周，副业机会雷达的第一版复盘。做对了什么，做错了什么，以及接下来要怎么做。
excerptEn: Two weeks from idea to launch — a retrospective on Prowl v1. What went right, what went wrong, and what's next.
tags: [Prowl, 独立开发, 产品复盘]
---

见微 Prowl 上线大概两个星期了。

流量不多，但来的人基本上都是真实有需求的——他们在认真找副业方向，不是随便逛逛。这让我觉得，方向是对的。

趁着还记得，写一下复盘。

---

## 为什么做 Prowl

起因很个人：我自己在找副业方向的时候，发现信息极其分散。

小红书上有人说做 AI 写作赚钱，知乎上有人说那是割韭菜；某个 Twitter 账号说独立开发一个月赚 2 万，评论区说他在撒谎。你永远不知道哪条信息是真实的、可验证的、适合你的。

我想要的是一个地方，能告诉我：**现在有哪些副业方向是真实可行的，有多少人在做，大概能赚多少，需要什么条件。**

这个东西不存在，所以我做了它。

---

## 技术栈和开发过程

技术栈很简单：**Next.js + Claude API + Vercel**。

没有数据库（第一版），内容是半静态的，定期用 Claude API 抓取和分析来自各渠道的信息，生成结构化的「机会卡片」。

从有想法到上线，总共花了约两周。其中：

- 第 1 天：确定核心功能，画线框图
- 第 2-5 天：Claude API 数据处理管道
- 第 6-9 天：前端页面（大量使用 Claude Code）
- 第 10-12 天：内容填充 + 调试
- 第 13-14 天：部署 + 域名配置 + 灰度测试

用 Claude Code 写前端代码的效率比我预期的高很多。很多组件我只需要描述清楚「我想要什么效果」，它就能给出 90% 可用的代码，我再调整细节。

---

## 做对的事

**定位很清晰。** 「帮你找 AI 时代真实可行的赚钱方向」这句话，来的用户基本上一看就懂我在做什么。

**先做最小可用产品。** 第一版只有最核心的功能——机会列表 + 详情，没有用户系统，没有收藏，没有推荐算法。这让我能在两周内上线，而不是憋着等半年。

**用 AI 做 AI 产品。** 整个开发过程大量使用了 Claude Code 和 Claude API，节省了大量时间，也让我更深入地理解了 LLM 工具链的实际能力和局限。

---

## 做错的事

**没有想清楚获客。** 上线之后发现，产品本身做得还可以，但根本不知道用户从哪里来。依赖自然流量完全不够，需要主动运营——但我没有提前规划这件事。

**内容质量参差不齐。** AI 生成的机会卡片有时候太泛，缺少真实的数据支撑。用户能看出来哪些是「AI 瞎编的」，这会损害信任感。

**UI 太赶。** 第一版的交互体验比较粗糙，很多细节没有打磨。这不是致命问题，但会让用户对产品的专业度产生怀疑。

---

## 接下来要做的事

按优先级：

1. **改善内容质量**——减少 AI 生成比例，加入更多可验证的真实案例和数据
2. **重做 UI**——尤其是移动端体验，现在在手机上看很难受
3. **建立运营节奏**——在小红书定期发关于副业方向的内容，把流量引回来

---

第一版上线只是开始。Prowl 能不能做成，取决于接下来我愿意在运营上投入多少。

技术是容易的部分。
```

- [ ] **Step 6: Create `content/posts/bytedance-week-one.md`**

```markdown
---
slug: bytedance-week-one
title: 字节实习第一周感受
titleEn: First Week at ByteDance
date: 2026-04-10
category: 碎碎念
categoryEn: Notes
categoryColor: "var(--color-accent-cyan)"
readTime: 6 min
excerpt: 进大厂第一周，代码、文档、会议、以及一些让我重新校准认知的时刻。
excerptEn: First week in a big tech company — code, docs, meetings, and a few moments that recalibrated my sense of what good looks like.
tags: [字节, 实习, 碎碎念]
---

在字节实习的第一周结束了。

坐在回家的地铁上，我脑子里还在跑白天看到的那段代码。不是因为它多复杂，而是因为它写得太干净了，让我有点自我怀疑。

---

## 第一天：信息过载

入职流程很顺畅，发设备、配环境、看文档。

但「看文档」这件事让我卡住了大半天。字节的内部文档系统（飞书文档）里存着海量的资料——项目背景、技术方案、历史决策记录、FAQ。

我一个下午跳了三十多个链接，看了大概五分之一，根本没看完。

后来我明白了：文档不是要在第一天全部读完的，它是你遇到问题时的查询系统。这个认知的调整让我轻松了很多。

---

## 让我印象最深的：代码 Review 文化

第一个任务是一个小需求，改一个已有组件的交互逻辑。

我写完提了 PR，等来的第一个 Review 评论是一串问题：

- 这个状态为什么放在父组件而不是子组件？
- 这里的 useEffect 依赖项是否完整？
- 这个边界情况（用户在请求中途刷新页面）有没有处理？

每一条都有道理。我之前写 PR 从来没被这样问过，一下子有点手足无措。

但也正因为如此，我改完之后感觉学到了东西——不只是把代码改对，而是真的理解了为什么要这样写。

---

## 工程规范让我重新校准了认知

在学校里，「好代码」对我来说大概是：能跑、没有明显 bug、逻辑清晰。

在字节，好代码的标准高了很多层：

- **可读性**：变量命名要能让三个月后的你看懂
- **可测试性**：写代码的同时就要考虑测试怎么写
- **边界情况**：每一个可能出错的地方都要明确处理
- **一致性**：和团队其他代码保持统一的风格

这不是苛刻，这是工程化。我之前的代码和这个标准比，差距很明显。

---

## 一点出乎意料的事

我以为大厂会很「流水线」——每个人只需要做好自己那块，其他的不用管。

实际情况是，团队内部讨论非常多。一个功能的方案，同事之间会反复拉齐，把潜在问题提前暴露出来，而不是做完再发现。

这种文化让我觉得，我的思考是被需要的，不只是我的双手。

---

## 第一周的感受总结

**学到了**：工程规范是什么、一个有经验的工程师是怎么看问题的、如何在一个复杂系统里快速定位代码。

**不适应的**：信息密度太高，每天结束都感觉脑子里装满了。

**最担心的**：实习结束之后，转正之后，会不会渐渐失去对外部世界的好奇心，变成只会在公司系统里高效运转的工具人。

这件事我没有答案，但我想把这个问题一直放在心里。
```

- [ ] **Step 7: Commit all articles**

```bash
git add content/
git commit -m "feat: add 5 blog articles in Markdown"
```

---

## Task 5: Create `components/PostCard.tsx`

**Files:**
- Create: `components/PostCard.tsx`

- [ ] **Step 1: Create the file**

```tsx
'use client'
import Link from 'next/link'
import { useLang } from '@/components/LangProvider'
import type { Post } from '@/lib/posts'

export default function PostCard({ post }: { post: Post }) {
  const { lang } = useLang()
  const title   = lang === 'zh' ? post.title   : post.titleEn
  const excerpt = lang === 'zh' ? post.excerpt  : post.excerptEn
  const cat     = lang === 'zh' ? post.category : post.categoryEn

  return (
    <Link href={`/blog/${post.slug}`} className="group block border-[2.5px] p-6 transition-all"
      style={{
        borderColor: 'var(--color-ink)',
        backgroundColor: 'var(--color-paper)',
        backdropFilter: 'blur(2px)',
        boxShadow: '5px 5px 0 var(--color-ink)',
      }}
      onMouseEnter={e => (e.currentTarget.style.transform = 'translateY(-3px)')}
      onMouseLeave={e => (e.currentTarget.style.transform = '')}>
      {/* Category */}
      <span className="mb-3 inline-block border-[1.5px] px-[6px] py-[2px] font-mono text-[8px] font-black uppercase tracking-[2px]"
        style={{ borderColor: post.categoryColor, color: post.categoryColor }}>
        {cat}
      </span>
      {/* Title */}
      <h2 className="mb-3 text-[18px] font-black leading-[1.25]" style={{ color: 'var(--color-ink)' }}>
        {title}
      </h2>
      {/* Excerpt */}
      {excerpt && (
        <p className="mb-4 text-[13px] leading-[1.7] opacity-55" style={{ color: 'var(--color-ink)' }}>
          {excerpt}
        </p>
      )}
      {/* Meta */}
      <div className="flex items-center gap-3">
        <span className="font-mono text-[10px] opacity-40" style={{ color: 'var(--color-ink)' }}>{post.date}</span>
        {post.readTime && (
          <>
            <span className="opacity-20" style={{ color: 'var(--color-ink)' }}>·</span>
            <span className="font-mono text-[10px] opacity-40" style={{ color: 'var(--color-ink)' }}>{post.readTime} read</span>
          </>
        )}
      </div>
    </Link>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add components/PostCard.tsx
git commit -m "feat: add PostCard client component"
```

---

## Task 6: Create `app/blog/page.tsx`

**Files:**
- Create: `app/blog/page.tsx`

- [ ] **Step 1: Create the file**

```tsx
import Nav from '@/components/Nav'
import PostCard from '@/components/PostCard'
import { getAllPosts } from '@/lib/posts'

export const metadata = {
  title: 'Blog — Sanli',
  description: '文章与碎碎念',
}

export default function BlogPage() {
  const posts = getAllPosts()

  return (
    <main>
      <Nav />
      <section className="px-8 py-[50px]">
        {/* Header */}
        <div className="mb-10 flex items-baseline gap-4">
          <span className="font-mono text-[11px] font-black uppercase tracking-[3px] opacity-30"
            style={{ color: 'var(--color-ink)' }}>§ 04</span>
          <h1 className="text-[36px] font-black"
            style={{ color: 'var(--color-ink)', borderBottom: '4px solid var(--color-accent-cyan)', paddingBottom: '2px' }}>
            博客
          </h1>
        </div>

        {/* Grid */}
        <div className="grid gap-[14px] md:grid-cols-2 lg:grid-cols-3">
          {posts.map(post => (
            <PostCard key={post.slug} post={post} />
          ))}
        </div>
      </section>

      {/* Footer */}
      <footer className="flex items-center justify-between px-8 py-[14px] font-mono text-[10px] font-bold uppercase tracking-[2px]"
        style={{ backgroundColor: 'var(--color-ink)', color: 'var(--color-bg)', borderTop: '3px solid var(--color-ink)' }}>
        <span>© 2026 Sanli</span>
        <span>Next.js · Tailwind · Framer Motion</span>
      </footer>
    </main>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add app/blog/page.tsx
git commit -m "feat: add /blog list page"
```

---

## Task 7: Create `components/BlogPostHeader.tsx` and `app/blog/[slug]/page.tsx`

**Files:**
- Create: `components/BlogPostHeader.tsx`
- Create: `app/blog/[slug]/page.tsx`

- [ ] **Step 1: Create `components/BlogPostHeader.tsx`**

```tsx
'use client'
import Link from 'next/link'
import { useLang } from '@/components/LangProvider'
import type { Post } from '@/lib/posts'

export default function BlogPostHeader({ post }: { post: Post }) {
  const { lang } = useLang()
  const title = lang === 'zh' ? post.title   : post.titleEn
  const cat   = lang === 'zh' ? post.category : post.categoryEn

  return (
    <>
      {/* Back */}
      <Link href="/blog"
        className="mb-8 inline-flex items-center gap-2 font-mono text-[11px] font-black uppercase tracking-[2px] opacity-50 transition-opacity hover:opacity-100"
        style={{ color: 'var(--color-ink)' }}>
        ← {lang === 'zh' ? '所有文章' : 'All Posts'}
      </Link>

      {/* Category */}
      <div className="mb-4">
        <span className="inline-block border-[1.5px] px-[6px] py-[2px] font-mono text-[9px] font-black uppercase tracking-[2px]"
          style={{ borderColor: post.categoryColor, color: post.categoryColor }}>
          {cat}
        </span>
      </div>

      {/* Title */}
      <h1 className="mb-5 text-[clamp(28px,5vw,52px)] font-black leading-[1.1] tracking-[-1px]"
        style={{ color: 'var(--color-ink)', fontFamily: '"Fusion Pixel", "VCR OSD Neue", ui-monospace, monospace' }}>
        {title}
      </h1>

      {/* Meta */}
      <div className="mb-6 flex flex-wrap items-center gap-3 font-mono text-[11px] opacity-45"
        style={{ color: 'var(--color-ink)' }}>
        <span>{post.date}</span>
        {post.readTime && <><span>·</span><span>{post.readTime} read</span></>}
        {post.tags.map(tag => (
          <span key={tag} className="border-[1.5px] px-2 py-[2px] text-[9px]"
            style={{ borderColor: 'var(--color-ink)' }}>{tag}</span>
        ))}
      </div>

      {/* EN notice */}
      {lang === 'en' && (
        <p className="mb-6 border-l-4 pl-4 font-mono text-[11px] opacity-50"
          style={{ borderColor: 'var(--color-accent-cyan)', color: 'var(--color-ink)' }}>
          本文以中文撰写 · This article is written in Chinese.
        </p>
      )}

      <hr style={{ border: 'none', borderTop: '3px solid var(--color-ink)', marginBottom: '2.5rem', opacity: 0.15 }} />
    </>
  )
}
```

- [ ] **Step 2: Create `app/blog/[slug]/page.tsx`**

```tsx
import { notFound } from 'next/navigation'
import Nav from '@/components/Nav'
import BlogPostHeader from '@/components/BlogPostHeader'
import { getAllPosts, getPost } from '@/lib/posts'

export async function generateStaticParams() {
  return getAllPosts().map(post => ({ slug: post.slug }))
}

export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }) {
  const { slug } = await params
  try {
    const post = getPost(slug)
    return { title: `${post.title} — Sanli`, description: post.excerpt }
  } catch {
    return { title: 'Post Not Found' }
  }
}

export default async function PostPage({ params }: { params: Promise<{ slug: string }> }) {
  const { slug } = await params
  let post
  try {
    post = getPost(slug)
  } catch {
    notFound()
  }

  return (
    <main>
      <Nav />
      <article className="px-8 py-[50px]">
        <div className="mx-auto max-w-[720px]">
          <BlogPostHeader post={post} />
          <div
            className="prose"
            dangerouslySetInnerHTML={{ __html: post.content! }}
          />
        </div>
      </article>

      <footer className="flex items-center justify-between px-8 py-[14px] font-mono text-[10px] font-bold uppercase tracking-[2px]"
        style={{ backgroundColor: 'var(--color-ink)', color: 'var(--color-bg)', borderTop: '3px solid var(--color-ink)' }}>
        <span>© 2026 Sanli</span>
        <span>Next.js · Tailwind · Framer Motion</span>
      </footer>
    </main>
  )
}
```

- [ ] **Step 3: Commit**

```bash
git add components/BlogPostHeader.tsx app/blog/
git commit -m "feat: add /blog/[slug] post detail page"
```

---

## Task 8: Update `lib/content.ts` Article URLs

**Files:**
- Modify: `lib/content.ts`

- [ ] **Step 1: Update URLs in the ARTICLES array**

Replace all `url: '#'` entries with their corresponding blog paths:

```typescript
// lib/content.ts — ARTICLES array, updated urls:
{ ..., url: '/blog/ai-exit-path' },        // AI 时代副业脱身路径推演
{ ..., url: '/blog/30-days-countdown' },   // 入职倒计时 30 天
{ ..., url: '/blog/claude-code-thesis' },  // 用 Claude Code 做毕业论文格式化
{ ..., url: '/blog/prowl-retrospective' }, // 见微 Prowl 上线复盘
{ ..., url: '/blog/bytedance-week-one' },  // 字节实习第一周感受
```

- [ ] **Step 2: Commit**

```bash
git add lib/content.ts
git commit -m "feat: link Insights articles to blog pages"
```

---

## Task 9: Verify and Final Push

- [ ] **Step 1: Start dev server and verify**

```bash
npm run dev
```

Check:
- `http://localhost:3000/blog` — list page renders, cards show titles
- `http://localhost:3000/blog/ai-exit-path` — post renders with prose styles
- Click EN toggle — titles/excerpt swap to English
- Click dark toggle — dark mode applies to prose content
- Homepage Insights section — article links point to `/blog/*`

- [ ] **Step 2: Final commit and push**

```bash
git push
```
